// Stub — wired in Task 32. Two render modes:
//   - mode="page": full-page detail at /programs/:id (direct navigation).
//   - mode="modal": dialog overlay; the underlying page renders behind it.
export function ProgramDetailRoute({ mode: _mode }: { mode: 'page' | 'modal' }) {
  return null;
}
